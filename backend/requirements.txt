fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
httpx>=0.27,<1
pydantic>=2.10,<3
# Baseline page-aware PDF parser. PyMuPDF is an optional higher-fidelity
# accelerator; the parser still degrades gracefully when it is unavailable.
pypdf>=5,<6
# pymupdf>=1.24,<2
# PyPDF2>=3,<4
